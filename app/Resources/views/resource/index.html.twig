{% extends ':Admin:base.html.twig' %}

{% block title %}{{ 'resource.list.title'|trans({}, 'UnisystemResourceBundle') }}{% endblock %}

{% block body %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{% include ':Admin:icon.html.twig' %}{{ 'resource.list.title'|trans({}, 'UnisystemResourceBundle') }}</h1>
                {% include ':Admin:flashbag.html.twig' %}
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="btn-toolbar">
                            <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalNew">{{ icon('plus fa-fw') }}{{ 'resource.new.link'|trans({}, 'UnisystemResourceBundle') }}</button>
                        </div>
                        <div class="clearfix"></div>
                    </div>
                    <div class="table-responsive">
                        <table class="records_list table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>{{ 'resource.form.photography'|trans({}, 'UnisystemResourceBundle') }}</th>
                                    <th>{{ knp_pagination_sortable(resources, 'resource.form.name'|trans({}, 'UnisystemResourceBundle'), 'name') }}{% if sort == 'name' %}{% if direction == 'desc' %}{{ icon('caret-down') }}{% else %}{{ icon('caret-up') }}{% endif %}{% endif %}</th>
                                    <th>{{ 'resource.form.actions'|trans({}, 'UnisystemResourceBundle') }}</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for key, resource in resources %}
                                <tr>
                                    <td>{% if resource.photography %}<img class="media-object img-rounded" src="{{ vich_uploader_asset(resource, 'file', 'Unisystem\\ResourceBundle\\Entity\\Resource')|imagine_filter('resource_photography_small') }}" alt="{{ 1 }}" />{% endif %}</td>
                                    <td>{% if resource.name %}{{ resource.name }}{% endif %}</td>
                                    <td>
                                        <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalEdit{{ resource.id }}">{{ icon('edit fa-fw') }}{{ 'resource.edit.link'|trans({}, 'UnisystemResourceBundle') }}</button>
                                        <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modalDelete{{ resource.id }}">{{ icon('times fa-fw') }}{{ 'resource.delete.link'|trans({}, 'UnisystemResourceBundle') }}</button>
                                    </td>
                                </tr>
                                <div class="modal fade" id="modalEdit{{ resource.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                    <div class="modal-dialog modal-lg" role="document">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                                <h4 class="modal-title" id="myModalLabel">{{ 'resource.edit.title'|trans({}, 'UnisystemResourceBundle') }}</h4>
                                            </div>
                                            <div class="modal-body">
                                                <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                                                    {% if resource.photography %}
                                                        <img class="img-responsive img-rounded" src="{{ vich_uploader_asset(resource, 'file')|imagine_filter('resource_photography_medium') }}" alt="{{ 1 }}" />
                                                    {% else %}
                                                        <img class="img-responsive img-rounded" src="{{ 'default'|imagine_filter('resource_photography_medium') }}" alt="{{ 1 }}" />
                                                    {% endif %}
                                                </div>
                                                <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                                                    {% set editForm = attribute(editForms, key) %}
                                                    {{ form_start(editForm) }}
                                                        {{ form_widget(editForm) }}
                                                        <button type="submit" class="btn btn-primary pull-right">{{ icon('check fa-fw') }}{{ 'resource.edit.confirm'|trans({}, 'UnisystemResourceBundle') }}</button>
                                                    {{ form_end(editForm) }}
                                                </div>
                                                <div class="clearfix"></div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                                <div class="modal fade" id="modalDelete{{ resource.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                    <div class="modal-dialog modal-md" role="document">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                                <h4 class="modal-title" id="myModalLabel">{{ 'resource.delete.title'|trans({}, 'UnisystemResourceBundle') }}</h4>
                                            </div>
                                            <div class="modal-body">
                                                <div class="col-xs-2 text-center">{{ icon('exclamation-triangle fa-5x') }}</div>
                                                <div class="col-xs-10"><p>{{ 'resource.delete.message'|trans({}, 'UnisystemResourceBundle') }}</p></div>
                                                <div class="clearfix"></div>
                                            </div>
                                            <div class="modal-footer">
                                                {% set deleteForm = attribute(deleteForms, key) %}
                                                {{ form_start(deleteForm) }}
                                                    {{ form_widget(deleteForm) }}
                                                    <button type="submit" class="btn btn-danger pull-right">{{ icon('times fa-fw') }}{{ 'resource.delete.confirm'|trans({}, 'UnisystemResourceBundle') }}</button>
                                                {{ form_end(deleteForm) }}
                                                <div class="clearfix"></div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
{#}
                    <div class="panel-body">
                        {% for key, resource in resources %}
                            <div class="col-xs-12 col-sm-12 col-md-6">
                                <div class="panel panel-default">
                                    <div class="panel-body">
                                        <div class="media">
                                            <div class="media-left">
                                                {% if resource.photography %}<img class="media-object img-rounded" src="{{ vich_uploader_asset(resource, 'file', 'Unisystem\\ResourceBundle\\Entity\\Resource')|imagine_filter('resource_photography_small') }}" alt="{{ 1 }}" />{% endif %}
                                            </div>
                                            <div class="media-body">
                                                <h4 class="media-heading">{% if resource.name %}{{ resource.name }}{% else %}&nbsp;{% endif %}</h4>
                                                <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#modalEdit{{ resource.id }}">{{ icon('edit fa-fw') }}{{ 'resource.edit.link'|trans({}, 'UnisystemResourceBundle') }}</button>
                                                <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modalDelete{{ resource.id }}">{{ icon('times fa-fw') }}{{ 'resource.delete.link'|trans({}, 'UnisystemResourceBundle') }}</button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="modal fade" id="modalEdit{{ resource.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                <div class="modal-dialog modal-lg" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                            <h4 class="modal-title" id="myModalLabel">{{ 'resource.edit.title'|trans({}, 'UnisystemResourceBundle') }}</h4>
                                        </div>
                                        <div class="modal-body">
                                            <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                                                {% if resource.photography %}
                                                    <img class="img-responsive img-rounded" src="{{ vich_uploader_asset(resource, 'file')|imagine_filter('resource_photography_medium') }}" alt="{{ 1 }}" />
                                                {% else %}
                                                    <img class="img-responsive img-rounded" src="{{ 'default'|imagine_filter('resource_photography_medium') }}" alt="{{ 1 }}" />
                                                {% endif %}
                                            </div>
                                            <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6">
                                                {% set editForm = attribute(editForms, key) %}
                                                {{ form_start(editForm) }}
                                                    {{ form_widget(editForm) }}
                                                    <button type="submit" class="btn btn-primary pull-right">{{ icon('check fa-fw') }}{{ 'resource.edit.confirm'|trans({}, 'UnisystemResourceBundle') }}</button>
                                                {{ form_end(editForm) }}
                                            </div>
                                            <div class="clearfix"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="modal fade" id="modalDelete{{ resource.id }}" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                                <div class="modal-dialog modal-md" role="document">
                                    <div class="modal-content">
                                        <div class="modal-header">
                                            <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                            <h4 class="modal-title" id="myModalLabel">{{ 'resource.delete.title'|trans({}, 'UnisystemResourceBundle') }}</h4>
                                        </div>
                                        <div class="modal-body">
                                            <div class="col-xs-2 text-center">{{ icon('exclamation-triangle fa-5x') }}</div>
                                            <div class="col-xs-10"><p>{{ 'resource.delete.message'|trans({}, 'UnisystemResourceBundle') }}</p></div>
                                            <div class="clearfix"></div>
                                        </div>
                                        <div class="modal-footer">
                                            {% set deleteForm = attribute(deleteForms, key) %}
                                            {{ form_start(deleteForm) }}
                                                {{ form_widget(deleteForm) }}
                                                <button type="submit" class="btn btn-danger pull-right">{{ icon('times fa-fw') }}{{ 'resource.delete.confirm'|trans({}, 'UnisystemResourceBundle') }}</button>
                                            {{ form_end(deleteForm) }}
                                            <div class="clearfix"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                        {% endfor %}
                    </div>
{#}
                    <div class="panel-footer">
                        <div class="text-center">
                            {{ knp_pagination_render(resources) }}
                        </div>
                        <div class="clearfix"></div>
                    </div>
                </div>
                <div class="modal fade" id="modalNew" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                    <div class="modal-dialog modal-sm" role="document">
                        <div class="modal-content">
                            <div class="modal-header">
                                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                <h4 class="modal-title" id="myModalLabel">{{ 'resource.new.title'|trans({}, 'UnisystemResourceBundle') }}</h4>
                            </div>
                            <div class="modal-body">
                                {{ form_start(newForm) }}
                                    {{ form_widget(newForm) }}
                                    <button type="submit" class="btn btn-primary pull-right">{{ icon('check fa-fw') }}{{ 'resource.new.confirm'|trans({}, 'UnisystemResourceBundle') }}</button>
                                {{ form_end(newForm) }}
                                <div class="clearfix"></div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->

{% endblock %}
