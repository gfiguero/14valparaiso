{% extends ':Admin:base.html.twig' %}

{% block title %}{{ 'noticecategory.list.title'|trans({}, 'UnisystemNoticeBundle') }}{% endblock %}

{% block body %}
    <div id="page-wrapper">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">{% include ':Admin:icon.html.twig' %}{{ 'noticecategory.list.title'|trans({}, 'UnisystemNoticeBundle') }}</h1>
                {% include ':Admin:flashbag.html.twig' %}
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <div class="btn-toolbar">
                            <a class="btn btn-primary" href="{{ path('notice_category_new') }}">{{ icon('file') }}&nbsp;{{ 'noticecategory.new.link'|trans({}, 'UnisystemNoticeBundle') }}</a>
                        </div>
                        <div class="clearfix"></div>
                    </div>
                    <div class="table-responsive">
                        <table class="records_list table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>{{ knp_pagination_sortable(noticeCategories, 'noticecategory.form.name'|trans({}, 'UnisystemNoticeBundle'), 'name') }}{% if sort == 'name' %}{% if direction == 'desc' %}{{ icon('caret-down') }}{% else %}{{ icon('caret-up') }}{% endif %}{% endif %}</th>
                                    <th>{{ knp_pagination_sortable(noticeCategories, 'noticecategory.form.createdAt'|trans({}, 'UnisystemNoticeBundle'), 'createdAt') }}{% if sort == 'createdAt' %}{% if direction == 'desc' %}{{ icon('caret-down') }}{% else %}{{ icon('caret-up') }}{% endif %}{% endif %}</th>
                                    <th>{{ knp_pagination_sortable(noticeCategories, 'noticecategory.form.updatedAt'|trans({}, 'UnisystemNoticeBundle'), 'updatedAt') }}{% if sort == 'updatedAt' %}{% if direction == 'desc' %}{{ icon('caret-down') }}{% else %}{{ icon('caret-up') }}{% endif %}{% endif %}</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for noticeCategory in noticeCategories %}
                                <tr class='clickableRow' href="{{ path('notice_category_show', { 'id': noticeCategory.id }) }}">
                                    <td>{% if noticeCategory.name %}{{ noticeCategory.name }}{% endif %}</td>
                                    <td>{% if noticeCategory.createdAt %}{{ noticeCategory.createdAt|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "dd-LLL-Y - HH:mm") }}{% endif %}</td>
                                    <td>{% if noticeCategory.updatedAt %}{{ noticeCategory.updatedAt|localizeddate('full', 'full', 'es_ES', 'America/Santiago', "dd-LLL-Y - HH:mm") }}{% endif %}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <div class="panel-footer">
                        <div class="text-center">
                            {{ knp_pagination_render(noticeCategories) }}
                        </div>
                        <div class="clearfix"></div>
                    </div>
                </div>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /#page-wrapper -->

{% endblock %}
